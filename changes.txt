Improvements:
    + Answer structure has been improved

Changes:
    + In the configuration file, the "websocket" protocol setting has been moved to the "server" section

Fixes:
    + The "websocket" protocol could break the server in certain methods, causing it to go offline