Added:
    + Filter "$arr" - search of data in array

Fixes:
    + Filter "$only" & "$omit" when connecting to WebSocket, could send the server into offline mode if the arguments were inputted incorrectly